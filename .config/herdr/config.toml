# herdr config, ported from .config/tmux/tmux.conf.
# Documented defaults: herdr --default-config

onboarding = false

[theme]
name = "catppuccin-mocha"

[keys]
# Physical Ctrl+Space is translated to Ctrl+B in Ghostty for macOS.
prefix = "ctrl+b"

# tmux: bind -r f display-popup -E -w 80% -h 60% tmux-sessionizer
[[keys.command]]
key = "prefix+f"
type = "popup"
command = "herdr-sessionizer"
width = "80%"
height = "60%"

[ui.sound]
enabled = false
