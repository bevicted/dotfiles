# Global gitignore (git reads ~/.config/git/ignore by XDG default;
# also wired via core.excludesfile in .gitconfig).

# Agent working plans and tickets at the repository root
/.agents/

# ThePrimeagen/99 nvim plugin scratch dir (tmp_dir = './.99')
.99/

**/.claude/settings.local.json
