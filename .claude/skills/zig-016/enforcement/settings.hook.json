{
  "//": "Merge this into .claude/settings.json (project) or ~/.claude/settings.json (global).",
  "//_note": "Runs before every Write/Edit, in the main session and inside subagents. The script self-skips non-.zig targets. Requires jq; copy validate-zig.sh somewhere and make it executable (chmod +x).",
  "hooks": {
    "PreToolUse": [
      {
        "matcher": "Write|Edit",
        "hooks": [
          {
            "type": "command",
            "command": "$HOME/.claude/skills/zig-016/enforcement/validate-zig.sh"
          }
        ]
      }
    ]
  }
}
