# npm 12 requires explicit opt-ins for non-registry tarballs and install scripts.
# This policy is scoped to Pi's shared npm package directory.
allow-remote=all
allow-scripts=@ast-grep/cli@0.45.3,node-pty@1.1.0
